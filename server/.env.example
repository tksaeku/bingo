PORT=5174
NODE_ENV=development

# Firestore / Firebase configuration
# Provide Firestore credentials in one of two ways:
# Option A (recommended): point to a local service account JSON file
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/serviceAccount.json

# Option B: set the base64-encoded service account JSON
# FIREBASE_SERVICE_ACCOUNT=<base64-encoded-service-account-json>

# Optionally provide your Firebase project id (useful for some Firebase SDKs)
# FIREBASE_PROJECT_ID=sdfsdf

# Example (do NOT commit real credentials):
# FIREBASE_SERVICE_ACCOUNT=sdfds
